import React  from 'react';
import './styles.css';

const Loader = () => {
    return (
        <div className="app-loader">
            <p>Fetching data...</p>
        </div>
    );
};

export default Loader;
